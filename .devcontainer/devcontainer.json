{
  "name": "SpotRaceJava",
  "build": {
    "dockerfile": "Dockerfile"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "vmware.vscode-spring-boot",
        "redhat.java",
        "vscjava.vscode-maven"
      ]
    }
  },

  "forwardPorts": [8080],

  "postCreateCommand": "mkdir -p src/main/resources/certs && echo \"$JWT_PUBLIC_KEY\" > src/main/resources/certs/public.pem && echo \"$JWT_PRIVATE_KEY\" > src/main/resources/certs/private.pem && ./mvnw -q -DskipTests compile",

  "remoteUser": "vscode"
}